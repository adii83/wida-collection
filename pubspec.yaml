name: winda_collection
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.6
  shared_preferences: ^2.3.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  supabase_flutter: ^2.6.0
  uuid: ^4.4.2
  flutter_dotenv: ^5.1.0
  google_fonts: ^6.2.1
  geolocator: ^14.0.2
  permission_handler: ^12.0.1
  flutter_map: ^8.2.2
  latlong2: ^0.9.1



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

## no dependency_overrides

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/images/

